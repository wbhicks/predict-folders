apply plugin: 'java'

dependencies {
//	testCompile 'junit:junit:[4,)'
//	testCompile group: 'junit', name: 'junit', version: '4.+'
    testCompile fileTree(dir: '/some/path/to/JUnit', include: ['*.jar'])
    compile fileTree(dir: 'lib', include: ['*.jar'])
    compile fileTree(dir: '/some/path/to/Tomcat/apache-tomcat-7.0.34/lib', include: ['*.jar'])
}

test {
	testLogging {
		// Show that tests are run in the command-line output
		events 'started', 'passed'
	}
}

task echoForRun << {
	println 'java -Dswing.defaultlaf=com.sun.java.swing.plaf.gtk.GTKLo' +
		'okAndFeel -classpath build/libs/Hib324sp1_t2.jar:lib/commo' +
		'ns-lang3-3.1.jar:lib/commons-io-2.4.jar:lib/commo' +
		'ns-cli-1.2.jar pdf/LogShell bash -s -l disabled' +
		'\n\njava -classpath build/libs/Hib324sp1_t3.jar:lib/commo' +
		'ns-lang3-3.1.jar:lib/commons-csv-1.0.jar met/ArrSeria' +
		'lizer ask 3 "%7.4f, " 1.1 0.0 13.82 17.76 0.23 0.01'
}

/* Equivalent to
		task helloLong {
		    doLast { println 'Hello long world.' }}
*/
task helloShort << {
    println 'Hello short world.'
}
